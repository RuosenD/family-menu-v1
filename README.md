# Family Menu v1

这是可直接部署的静态网页成果包。

部署方式：

- 保留此文件夹内的完整目录结构
- 将此文件夹中的所有文件上传到静态服务器根目录或站点子目录
- 默认入口文件为 `index.html`

说明：

- 本成果包已经只保留当前网页实际运行所需文件
- 不依赖原 `order-menu` 目录中的其他文件
- 不包含历史预览脚本、旧版素材、未使用图片和临时文件

建议：

- 如果部署在子目录中，直接保持当前相对路径结构即可
- 如需更新内容，请基于此目录继续修改并重新部署
